server {

    listen 80;
    
    server_name {{ec2_instance.instances[0].public_ip_address}};

    root /var/www/html;

    index index.html;

    location / {
        try_files $uri /index.html;
    }

}